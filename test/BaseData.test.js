
describe('基础档案', () => {
  before(done => {
    handler.connect(done);
  })

  after(() => {
    //console.log(mongo.close)
    //  mongo.db.close();

  });

  it('创建一个部门档案，并添加部门和子部门基础数据', async () => {

  })

  it('创建存货档案，添加存货分类和存货数据', async () => {

  })

})
